#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

void printnum(int max);

int main() 
{
    int n;
    sacnf("%d", n);
    printnum(n);
    return 0;
}

void printnum(int max)
{
    if (max == 0)
        return;
}